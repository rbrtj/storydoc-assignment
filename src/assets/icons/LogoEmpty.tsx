export const LogoEmpty = ({ onClick }) => {
  return (
    <div
      style={{
        width: "32px",
        height: "32px",
        backgroundColor: "#B3C4D6",
        borderRadius: "8px",
      }}
      onClick={onClick}
    ></div>
  );
};
